language: java

script:
- ./gradlew --refresh-dependencies
- rm -v !("runwar-$version-SNAPSHOT.jar"|"runwar-$version.jar") 

deploy:
  # Upload binaries
  - provider: s3
    on:
      branch:
        - develop
    skip_cleanup: false
    access_key_id: $AWS_ACCESS_KEY
    secret_access_key: $AWS_ACCESS_SECRET
    bucket: "downloads.ortussolutions.com"
    local-dir: dist/libs/runwar-$version-SNAPSHOT.jar
    upload-dir: cfmlprojects/runwar/$version
    acl: public_read